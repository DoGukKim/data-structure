import { SingleLinkList } from "./single-linked-list";

export class Stack {
  constructor() {
    this.list = new SingleLinkList();
  }

  push(value) {
    this.list.push(value);

    return null;
  }

  size() {
    this.size();

    return null;
  }
}
